# Review required before journal upload

Everything below is either an open question that still needs a decision/confirmation, or a note about something that needs to be reflected in the manuscript text. Items that were fully resolved (bug fixed, file found, discrepancy explained, nothing further to do) have been removed from this list as the repository was cleaned up -- see `docs/MANIFEST.tsv` and each folder's own `README.md` for what the current, final state of every file is.

1. **U-Net architecture review.** `01_imaging/shriya_unet_myocardium.py` and `01_imaging/deploy_unet_segmentation.py` apply the second convolution in `conv2d_block` to `input_tensor` rather than to the first convolution's output `x`. Training and deployment definitions match, so the deployed weights are structurally consistent with an effective one-convolution-per-block graph, but the implementation differs from the apparent two-convolution intent. Confirm before changing code or Methods -- if the Methods describe two sequential convolutions per block, either the code or the text needs to change.

2. **MR-PRESSO manifest ambiguity.** `06_mr/mr_presso/run_MR_PRESSO.sh`'s SLURM array bound was corrected to `--array=1-20` to match `mrpresso_pairs_2.txt` (20 rows), which is the file it actually reads. `mrpresso_pairs.txt` (37 rows) is kept alongside as the other candidate manifest. **If `mrpresso_pairs.txt` was the one actually used for the reported run**, change `PAIRS_FILE` back to `mrpresso_pairs.txt` and `--array` to `1-37`.

3. **Rotate/revoke the Hugging Face token.** A live-looking Hugging Face API token was found in plain text in `01_imaging/deploy_fine_tune_sam_myocardium_autobounding_box.sh` and has been replaced with an `$HF_TOKEN` environment-variable reference -- but the token itself sat in a folder that was about to be pushed to GitHub. **Rotate or revoke that token** regardless of code changes.

4. **`run_MR_PRESSO.R` / `combine_MR_PRESSO.R` output-directory placeholder.** The original code was inconsistent about whether results go to `MRPRESSO_results` or `MRPRESSO_results_2`. Both scripts now read a `RESULTS_SUBDIR` placeholder (`"<RESULTS_SUBDIR>"`, overridable via `$MRPRESSO_RESULTS_SUBDIR`) instead of guessing. **Set this to whichever directory the actual reported MR-PRESSO results live in before running either script.**

5. **`vae_evaluation.py` has two structurally different source copies.** One version is wrapped in an `evaluate_vae()` function with `if __name__ == "__main__":`; the other is a flat top-level script with identical metric functions and CLI arguments. This repo keeps the function-wrapped version. The two are behaviorally equivalent as far as could be determined from source alone -- **confirm neither diverges in a way that changed reported metrics.**

6. **`06_mr/MR_job_shriya_2_latent.sh` -- path placeholder needs a real value.** Despite the `_latent` suffix, its `--brain_path` argument originally pointed at a T1-percentile GWAS directory, not a latent-dimension one. `--brain_path` and `--out_file` now use a `LATENT_GWAS_DIR="<LATENT_GWAS_DIR>"` placeholder. **Set it to the actual VAE-latent-dimension GWAS output directory** (e.g. what `05_gwas/gwas_VAE.sh` produces) before running this job.

7. **STRING-db, protein prioritization, and deCODE replication were manual/previously-scripted steps.** Per the repo owner, the STRING-db PPI analysis, the top-5/PP.H4 protein-prioritization selection, and the deCODE MR-replication comparison were all performed manually or by reusing previously-existing scripts, not by code that was ever meant to live in this repository (deCODE's MR run itself reused `06_mr/`'s existing MR scripts rather than a separate decode-specific script). No code changes are needed for any of these three -- **document them as manual/external steps in the manuscript Methods** rather than expecting to find dedicated scripts for them here.

8. **`04_pwas/03_PheWAS_delta_rank_test_T1_descriptive_statistics.ipynb` -- verify a fixed cell's output against the manuscript.** One cell computes the T1 descriptive-statistics text (mean T1 ± SD, skewness, segmentation success rate) in language that reads like manuscript Results prose, but it referenced two undefined variables and could never have run successfully. It's been fixed to the evidently-intended variables (`valid_images`, `unfiltered_unet_t1_raw`, both defined and used correctly elsewhere in the notebook). **Confirm the resulting printed numbers match what's reported in the manuscript** -- this output was never previously verified against the published text.

9. **`08_clinical_associations/02_mortality_curves_chi_squared.ipynb` -- a misleading "saved" message.** One of the four kept "nested Cox model comparison" versions (`penalizer=0.0`, "unpenalized for valid LRT") prints `Done. All results saved.` despite its save call being commented out. **Confirm before assuming that version's CSVs exist anywhere on disk.**

10. **`09_figures/01_Fibrosis_Figures.ipynb` -- two open questions.**
    - An 8-cell tangent compares the original burden-test run against a `_changedQC` rerun (`./data/burden_test_latent_1025_changedQC/`), a dataset that doesn't appear anywhere else in the repo. Worth checking whether that changed-QC rerun matters before treating it as unused exploration.
    - The notebook has two MR sections -- `# MR` (using `MR_T1_preprint_sig.txt`/`MR_latent_preprint_sig.txt`) and, later, `# Updated, more comprehensive MR Results` (comparing an "old" vs. a "new" MR hits file) -- kept as separate/complementary rather than merged, since both produce distinct saved figures/tables and it isn't clear from source alone that one supersedes the other. **Worth confirming against the manuscript** which result set (or both) is the one reported.
