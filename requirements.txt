# Python dependencies for the deep-learning cardiac MRI phenotyping and
# proteome-wide MR analysis pipeline.
#
# Versions below are pinned from the `keras_gpu` conda environment on the
# Stanford Sherlock cluster (Python 3.9.7) -- the primary environment, used
# for everything except the GPU training/deployment steps in 01_imaging/ and
# 02_vae/ (U-Net training/deployment, SAM fine-tuning, CVAE
# training/evaluation/deployment), which used a separate GPU environment --
# see requirements-gpu.txt.

# Requires Python 3.9.7 (install separately, e.g. via conda create -n <name> python=3.9.7)

# --- Core scientific / data handling ---
numpy==1.22.4
pandas==2.3.2
scipy==1.7.1
lifelines==0.30.0
scikit-learn==1.0.2
scikit-image==0.18.1
statsmodels==0.12.2
seaborn==0.13.2
matplotlib==3.5.1
matplotlib-venn

# --- Imaging (non-GPU: DICOM conversion, mask QC) ---
opencv-python-headless==4.11.0.86
Pillow==9.0.1
tifffile==2021.7.2
pydicom==2.4.4
tqdm==4.67.1
IPython==7.29.0

# --- Survival / clinical association analysis ---
kaplanmeier==0.2.0

# --- Clinical validation / ML ---
xgboost==2.1.4
shap==0.49.1
imbalanced-learn==0.12.4

# --- Dimensionality reduction / visualization (not found in the recorded environment -- confirm before submission) ---
umap-learn
plotly
ipywidgets
